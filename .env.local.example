# Come by Shop — Environment variables example
# Copy to .env.local and fill in your values

# Google Maps API key (for address autocomplete — Task 1)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=your_key_here

# External backend URL (leave empty to use built-in mock API)
# NEXT_PUBLIC_API_URL=https://your-backend.com
